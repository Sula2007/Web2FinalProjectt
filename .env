# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Get connection string from https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanager?retryWrites=true&w=majority

# JWT Configuration
# Generate random string (can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random
JWT_EXPIRE=7d

# Email Configuration (Brevo/SendinBlue)
# Register at https://app.brevo.com
# Go to Settings -> SMTP & API -> SMTP
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=a1e2a9001@smtp-brevo.com
SMTP_PASSWORD=1nZcK3kwASVGLzYO
SMTP_FROM=noreply@taskmaster.com

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Admin Configuration
# Email for first admin user
ADMIN_EMAIL=admin@taskmaster.com
